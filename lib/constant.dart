import 'package:flutter/material.dart';

const kTextEnterPassword = Text(
  'Enter Your email and we will send you a password reset link',
  textAlign: TextAlign.center,
  style: TextStyle(fontSize: 25),
);

const kTextResetPassword = Text(
  'Reset Password',
  textAlign: TextAlign.center,
  style: TextStyle(fontSize: 18, color: kColourWhite),
);

const kTextResetPasswordResent = Text(
  'Password resent to your email address',
  textAlign: TextAlign.center,
  style: TextStyle(fontSize: 18, color: kColourWhite),
);

const kSideBox30 = SizedBox(height: 30);

const kBorderSideWhite = BorderSide(color: Colors.white);
const kBorderSidePurple = BorderSide(color: Colors.deepPurple);

// Theme Color for the project
const kInactiveCardColour = Color(0xFF111328);
const kBottomContainerColour = Color(0xFFEB1555);
const kColourWhite = Color(0xFFFFFFFF);
const kColourGray = Color(0xFF8D8E98);

const kLabelTexStyle = TextStyle(
  fontSize: 15.0,
  color: kColourGray,
);
